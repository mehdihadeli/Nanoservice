apiVersion: v1
kind: Pod
metadata:
  name: nanoservice-pod
  labels:
    name: nanoservice
spec:
  containers:
  - name: nano-service
    image: mehdihadeli/nano-service:dev
    ports:
    - containerPort: 5000